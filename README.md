
**data/**
- 실험에 사용할 입력 이미지 파일 (예: `sample.png` 등)

**logs/**
- 실험 로그 및 텍스트 리포트가 저장될 디렉터리 (`experiments/config.py`에서 경로 설정)

**figures/**
- 플롯(그래프) 결과 이미지가 저장될 디렉터리

**results/** (또는 `CSV_DIR`로 지정된 폴더)
- CSV 결과 및 통계 파일 저장 위치

**images/** (또는 `IMAGES_DIR`로 지정된 폴더)
- 원본/암호화/복호화 이미지 저장 위치

